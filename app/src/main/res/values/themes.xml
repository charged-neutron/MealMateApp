<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlatePal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PlatePal" parent="Base.Theme.PlatePal" />

    <style name="Toolbar.WhiteIcons" parent="">
        <!-- This color is used for the navigation icon and action icons -->
        <item name="colorControlNormal">@color/white</item>
    </style>

    <!-- Style for the detail items on the ViewCardFood screen -->
    <style name="DetailItemTextView" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:drawablePadding">12dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">?android:attr/textColorSecondary</item>
    </style>

    <!-- Style for the RatingBar -->
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/textSecondary</item>
        <item name="colorControlActivated">@color/accentColor</item>
    </style>

</resources>